include_rules

: foreach *.cpp |> !cpp $(XMLFLAGS) |>
: *.o NewNet/libNewNet.a utils/libutils.a efsw/lib/libefsw-static-release.a |> @(DEBUG) c++ $(XMLLINKS) -lz -levent -lnettle -ldb_cxx -lpthread -ltag -o %o %f |> newsoul
